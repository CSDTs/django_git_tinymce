language: python

python:
  - 3.6

env:
  - DJANGO=1.11.2 DB=sqlite

install:
  - pip install flake8
  - pip install -r requirements.txt

before_script:
  # W191: indentation contains tabs

  flake8 --exclude=migrations --ignore=W191 --max-line-length=123 .

script:
  - python manage.py test
